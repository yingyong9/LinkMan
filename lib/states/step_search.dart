import 'package:flutter/material.dart';

class StepSearch extends StatelessWidget {
  const StepSearch({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
